{
  "scripts": {
    "test": "cypress run",
    "merge-reports": "mochawesome-merge cypress/reports/json/*.json -o cypress/reports/combined-report.json",
    "generate-html": "marge cypress/reports/combined-report.json -o cypress/reports -f index",
    "test:report": "npm run test && npm run merge-reports && npm run generate-html"
  },
  "devDependencies": {
    "cypress": "^15.7.1",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.0",
    "mochawesome-report-generator": "^6.3.2"
  },
  "dependencies": {
    "react": "^19.2.1"
  }
}
